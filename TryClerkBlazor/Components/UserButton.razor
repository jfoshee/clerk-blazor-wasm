@inject IClerkAuthService Clerk

<div @ref="elementRef"></div>

@code {
    private ElementReference elementRef;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await Clerk.MountUserButtonAsync(elementRef);
        }
    }
}
